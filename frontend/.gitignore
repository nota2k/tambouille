# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
.env
# dist/ est versionné sur la branche o2switch-db : la prod le récupère par
# `git pull`, il n'y a pas d'étape de build côté serveur. L'ignorer obligeait à
# un `git add -f` à chaque fois, et un build oublié partait donc en silence.
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

.eslintcache

# Cypress
/cypress/videos/
/cypress/screenshots/

# Vitest
__screenshots__/

# Vite
*.timestamp-*-*.mjs

tmp/